<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ============================================ -->
    <!-- APPENDER: Console legivel (dev/default)      -->
    <!-- ============================================ -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================================ -->
    <!-- APPENDER: JSON estruturado (producao/ELK)    -->
    <!-- ============================================ -->
    <appender name="LOGSTASH" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app":"nexus","environment":"producao"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger_name</logger>
                <thread>thread_name</thread>
                <level>level</level>
                <stackTrace>stack_trace</stackTrace>
            </fieldNames>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>30</maxDepthPerThrowable>
                <maxLength>2048</maxLength>
                <rootCauseFirst>true</rootCauseFirst>
            </throwableConverter>
        </encoder>
    </appender>

    <!-- ============================================ -->
    <!-- LOGGERS COMUNS (todos os perfis)             -->
    <!-- ============================================ -->

    <!-- Suprime WARNs de HTTP method not supported (health checks, bots, etc) -->
    <logger name="org.springframework.web.servlet.mvc.support.DefaultHandlerExceptionResolver" level="ERROR"/>

    <!-- Suprime logs verbosos do Spring Security -->
    <logger name="org.springframework.security" level="ERROR"/>

    <!-- Suprime logs verbosos do Tomcat/WebSocket -->
    <logger name="org.apache.tomcat" level="ERROR"/>
    <logger name="org.apache.catalina" level="ERROR"/>

    <!-- Modulo validacao-ad -->
    <logger name="keysson.apis.validacaoad" level="INFO"/>

    <!-- Modulo empresa -->
    <logger name="keysson.apis.empresa" level="INFO"/>

    <!-- HikariCP: mantem WARN para monitoramento de pool -->
    <logger name="com.zaxxer.hikari" level="WARN"/>

    <!-- Hibernate/SQL: suprime verboso, mantem erros -->
    <logger name="org.hibernate" level="ERROR"/>
    <logger name="org.hibernate.engine.jdbc.spi.SqlExceptionHelper" level="WARN"/>
    <logger name="org.hibernate.event.internal" level="ERROR"/>

    <!-- PostgreSQL driver -->
    <logger name="org.postgresql" level="WARN"/>

    <!-- Spring Boot startup: permite INFOs importantes -->
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.context" level="INFO"/>

    <!-- ============================================ -->
    <!-- PERFIL: default/dev — formato legivel        -->
    <!-- ============================================ -->
    <springProfile name="default">
        <logger name="keysson.apis.estoque" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ============================================ -->
    <!-- PERFIL: prod — formato JSON (ELK/Loki)      -->
    <!-- ============================================ -->
    <springProfile name="prod">
        <logger name="keysson.apis.estoque" level="WARN"/>
        <root level="INFO">
            <appender-ref ref="LOGSTASH"/>
        </root>
    </springProfile>

</configuration>
