apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: empresa-servicemonitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: empresa
  namespaceSelector:
    matchNames:
      - producao
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s